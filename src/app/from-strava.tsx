import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { StravaImportFlow } from '@/ui/exercise/StravaImportFlow';

export default function FromStravaRoute() {
  return (
    <MockupScreen>
      <StravaImportFlow />
    </MockupScreen>
  );
}
