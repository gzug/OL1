import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { SubscriptionScreen } from '@/ui/settings/PlainScreens';

export default function Route() {
  return (
    <MockupScreen>
      <SubscriptionScreen />
    </MockupScreen>
  );
}
