import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { HubsScreen } from '@/ui/settings/HubsScreen';

export default function Route() {
  return (
    <MockupScreen>
      <HubsScreen />
    </MockupScreen>
  );
}
