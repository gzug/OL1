import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { CoachesScreen } from '@/ui/settings/CoachesScreen';

export default function Route() {
  return (
    <MockupScreen>
      <CoachesScreen />
    </MockupScreen>
  );
}
