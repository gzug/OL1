import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { NotificationsScreen } from '@/ui/settings/PlainScreens';

export default function Route() {
  return (
    <MockupScreen>
      <NotificationsScreen />
    </MockupScreen>
  );
}
