import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { AboutScreen } from '@/ui/settings/PlainScreens';

export default function Route() {
  return (
    <MockupScreen>
      <AboutScreen />
    </MockupScreen>
  );
}
