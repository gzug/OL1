import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { GoalsScreen } from '@/ui/settings/GoalsScreen';

export default function Route() {
  return (
    <MockupScreen>
      <GoalsScreen />
    </MockupScreen>
  );
}
