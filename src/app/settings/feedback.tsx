import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { FeedbackScreen } from '@/ui/settings/PlainScreens';

export default function Route() {
  return (
    <MockupScreen>
      <FeedbackScreen />
    </MockupScreen>
  );
}
