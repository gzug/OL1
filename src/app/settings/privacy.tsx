import { MockupScreen } from '@/ui/mockup/MockupScreen';
import { PrivacyScreen } from '@/ui/settings/PlainScreens';

export default function Route() {
  return (
    <MockupScreen>
      <PrivacyScreen />
    </MockupScreen>
  );
}
